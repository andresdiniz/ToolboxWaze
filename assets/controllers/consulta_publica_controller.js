import { Controller } from '@hotwired/stimulus';

export default class extends Controller {
    static targets = ['type', 'state', 'city', 'query', 'results', 'list', 'pagination', 'source', 'resultTitle', 'helper', 'map', 'mapButton'];

    connect() { this.type = this.typeTargets.find((button) => button.classList.contains('is-active'))?.dataset.type || 'radar'; this.page = 1; this.abortController = null; this.cityRequest = null; }
    selectType(event) { this.type = event.currentTarget.dataset.type; this.typeTargets.forEach((button) => { const active = button === event.currentTarget; button.classList.toggle('is-active', active); button.setAttribute('aria-selected', active ? 'true' : 'false'); }); this.page = 1; this.resetResults(); this.resetCity('Selecione o estado para carregar municípios'); if (this.stateTarget.value) this.loadCities(); }
    async loadCities() { const uf = this.stateTarget.value; this.cityRequest?.abort(); this.cityRequest = new AbortController(); this.resetCity(uf ? 'Carregando municípios com dados...' : 'Primeiro escolha o estado'); if (!uf) return; const url = new URL(this.element.dataset.municipiosUrl, window.location.origin); url.searchParams.set('tipo', this.type); url.searchParams.set('uf', uf); try { const response = await fetch(url, { signal: this.cityRequest.signal, headers: { Accept: 'application/json' } }); if (!response.ok) throw new Error('request'); const data = await response.json(); const cities = Array.isArray(data.items) ? data.items : []; if (!cities.length) { this.resetCity('Nenhum município com dados nesta categoria'); this.helperTarget.textContent = `Não há ${this.label()} cadastrados para este estado.`; return; } this.cityTarget.innerHTML = '<option value="">Selecione o município</option>' + cities.map((city) => `<option value="${this.escape(city)}">${this.escape(city)}</option>`).join(''); this.cityTarget.disabled = false; this.helperTarget.textContent = `Municípios com dados de ${this.label()} carregados.`; } catch (error) { if (error.name === 'AbortError') return; this.resetCity('Não foi possível carregar os municípios'); this.helperTarget.textContent = 'Verifique a conexão e tente novamente.'; } }
    stateChanged() { this.resetResults(); this.loadCities(); }
    cityChanged() { this.resetResults(); }
    async search() { if (!this.stateTarget.value || !this.cityTarget.value) { this.helperTarget.textContent = 'Escolha o estado e o município antes de consultar.'; return; } this.page = 1; await this.fetchResults(); }
    async fetchResults() { this.abortController?.abort(); this.abortController = new AbortController(); const url = new URL(`${this.element.dataset.searchBaseUrl}/${this.type}`, window.location.origin); url.searchParams.set('uf', this.stateTarget.value); url.searchParams.set('municipio', this.cityTarget.value); if (this.queryTarget.value.trim()) url.searchParams.set('q', this.queryTarget.value.trim()); url.searchParams.set('page', this.page); this.resultsTarget.hidden = false; this.listTarget.innerHTML = '<div class="public-result-card">Carregando resultados...</div>'; try { const response = await fetch(url, { signal: this.abortController.signal, headers: { Accept: 'application/json' } }); if (!response.ok) throw new Error('request'); this.render(await response.json()); } catch (error) { if (error.name !== 'AbortError') this.listTarget.innerHTML = '<div class="public-result-card"><h3>Não foi possível concluir a consulta</h3><p>Tente novamente em instantes.</p></div>'; } }
    render(data) { const total = Number(data.total || 0); this.resultTitleTarget.textContent = `${total} resultado${total === 1 ? '' : 's'}`; this.listTarget.innerHTML = data.items?.length ? data.items.map((item) => this.card(item)).join('') : '<div class="public-result-card"><h3>Nenhum resultado encontrado</h3><p>Tente alterar os filtros da consulta.</p></div>'; this.sourceTarget.textContent = `Fonte: ${data.source?.name || 'não informada'}. Última atualização disponível: ${data.source?.updatedAt || 'não informada pela fonte'}.`; this.renderPagination(data); this.mapButtonTarget.hidden = !(data.items || []).some((item) => item.latitude && item.longitude); }
    card(item) { if (this.type === 'posto') return this.postoCard(item); const title = this.escape(item.nome || item.tipo || 'Registro público'); const address = [item.endereco, item.municipio, item.uf].filter(Boolean).map((value) => this.escape(value)).join(', '); const details = [item.telefone, item.velocidade ? `${item.velocidade} km/h` : null, item.sentido].filter(Boolean).map((value) => this.escape(value)).join(' · '); return `<article class="public-result-card"><h3>${title}</h3>${address ? `<p>${address}</p>` : ''}${details ? `<p>${details}</p>` : ''}</article>`; }
    postoCard(item) { const titleValue = item.nomeFantasia || item.razaoSocial || 'Posto revendedor'; const title = this.escape(titleValue); const legal = item.nomeFantasia && item.razaoSocial ? `<p class="public-result-card__muted">${this.escape(item.razaoSocial)}</p>` : ''; const address = [item.endereco, item.complemento, item.bairro, item.municipio, item.uf, item.cep].filter(Boolean).map((value) => this.escape(value)).join(', '); const meta = [item.bandeira, item.cnpj ? `CNPJ: ${this.formatCnpj(item.cnpj)}` : null].filter(Boolean).map((value) => this.escape(value)).join(' · '); const dates = [item.autorizacao ? `Autorização: ${item.autorizacao}` : null, item.dataPublicacao ? `Publicação: ${item.dataPublicacao}` : null, item.dataVinculacao ? `Vinculação: ${item.dataVinculacao}` : null].filter(Boolean).map((value) => this.escape(value)).join(' · '); const identity = this.detailGrid([['Código ISIMP', item.codigoIsimp], ['CNPJ', item.cnpj ? this.formatCnpj(item.cnpj) : null], ['Bandeira', item.bandeira], ['Autorização', item.autorizacao]]); const location = this.detailGrid([['Endereço', item.endereco], ['Complemento', item.complemento], ['Bairro', item.bairro], ['CEP', item.cep], ['Município/UF', [item.municipio, item.uf].filter(Boolean).join(' / ')] ]); const datesGrid = this.detailGrid([['Data de publicação', item.dataPublicacao], ['Data de vinculação', item.dataVinculacao], ['Importado em', this.formatDate(item.importedAt)], ['Atualizado em', this.formatDate(item.updatedAt)] ]); return `<article class="public-result-card public-result-card--posto"><div class="public-result-card__top"><span class="public-result-card__badge">ANP</span><span class="public-result-card__code">${this.escape(item.codigoIsimp || '')}</span></div><h3>${title}</h3>${legal}<section class="public-detail-section"><h4>Identificação</h4><div class="public-detail-grid">${identity}</div></section><section class="public-detail-section"><h4>Localização</h4><div class="public-detail-grid">${location}</div></section><section class="public-detail-section"><h4>Situação e atualização</h4><div class="public-detail-grid">${datesGrid}</div></section></article>`; }
    detailGrid(items) { return items.filter(([, value]) => value !== null && value !== undefined && String(value).trim() !== '').map(([label, value]) => `<div class="public-detail-item"><span>${this.escape(label)}</span><strong>${this.escape(value)}</strong></div>`).join(''); }
    formatCnpj(value) { const digits = String(value).replace(/\D/g, ''); return digits.length === 14 ? digits.replace(/(\d{2})(\d{3})(\d{3})(\d{4})(\d{2})/, '$1.$2.$3/$4-$5') : value; }
    formatDate(value) { if (!value) return null; const date = new Date(value); return Number.isNaN(date.getTime()) ? value : date.toLocaleString('pt-BR'); }
    renderPagination(data) { const pages = Math.ceil(Number(data.total || 0) / Number(data.limit || 20)); if (pages <= 1) { this.paginationTarget.innerHTML = ''; return; } this.paginationTarget.innerHTML = Array.from({ length: pages }, (_, index) => `<button type="button" data-page="${index + 1}">${index + 1}</button>`).join(''); this.paginationTarget.querySelectorAll('button').forEach((button) => button.addEventListener('click', () => { this.page = Number(button.dataset.page); this.fetchResults(); })); }
    toggleMap() { this.mapTarget.hidden = !this.mapTarget.hidden; }
    resetResults() { this.resultsTarget.hidden = true; this.listTarget.innerHTML = ''; this.paginationTarget.innerHTML = ''; }
    resetCity(message) { this.cityTarget.innerHTML = `<option value="">${this.escape(message)}</option>`; this.cityTarget.disabled = true; }
    label() { return ({ radar: 'radares', escola: 'escolas', posto: 'postos' })[this.type] || 'registros'; }
    escape(value) { return String(value).replace(/[&<>'"]/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#039;', '"': '&quot;' }[char])); }
}
