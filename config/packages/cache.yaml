framework:
    cache:
        # Cache padrão para o sistema (opcional)
        app: cache.adapter.filesystem

        # Pool específico para dashboard
        pools:
            dashboard_cache:
                adapter: cache.adapter.filesystem
                default_lifetime: 300