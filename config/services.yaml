# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # ---------------------------------------------------------------
    # Controllers
    # ---------------------------------------------------------------

    # RegistrationController: MailerInterface removido, MessageBusInterface é autowired.
    # Nenhuma injeção explícita necessária aqui.

    # AdminUserController: usa MessageBusInterface (autowired) e emailQueueLogger (explícito)
    App\Controller\AdminUserController:
        arguments:
            $emailQueueLogger: '@monolog.logger.email_queue'

    # ---------------------------------------------------------------
    # MessageHandlers
    # ---------------------------------------------------------------

    App\MessageHandler\EnviarEmailContaHandler:
        arguments:
            $emailQueueLogger: '@monolog.logger.email_queue'
            $mailerFrom: '%env(MAILER_FROM)%'

    App\MessageHandler\EnviarEmailRadarVencidoHandler:
        arguments:
            $emailQueueLogger: '@monolog.logger.email_queue'
            $mailerFrom: '%env(MAILER_FROM)%'
            $appBaseUrl: '%env(APP_BASE_URL)%'
