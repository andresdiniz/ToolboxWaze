<!DOCTYPE html>
<html lang="pt-BR">
<head><meta charset="UTF-8"><title>Resposta na sua solicitação</title></head>
<body style="font-family:Arial,sans-serif;max-width:600px;margin:0 auto;padding:20px;color:#333">

  <div style="background:#0d6efd;padding:16px 24px;border-radius:6px 6px 0 0">
    <h1 style="color:#fff;margin:0;font-size:20px">ToolboxWaze</h1>
  </div>

  <div style="border:1px solid #dee2e6;border-top:none;padding:24px;border-radius:0 0 6px 6px">
    <p style="margin-top:0">Olá, <strong>{{ solicitacao.solicitanteNome }}</strong>!</p>

    <p>
      A equipe adicionou uma resposta na sua solicitação
      <strong>{{ solicitacao.tipoLabel }}</strong> (protocolo <strong>#{{ solicitacao.id }}</strong>).
    </p>

    {% set ultimoComentario = solicitacao.comentarios|filter(c => not c.interno)|last %}
    {% if ultimoComentario is defined %}
    <blockquote style="border-left:4px solid #0d6efd;margin:16px 0;padding:8px 16px;background:#f0f4ff;border-radius:0 4px 4px 0">
      {{ ultimoComentario.mensagem|nl2br }}
    </blockquote>
    {% endif %}

    <p>Acesse o link abaixo para ver todos os detalhes e responder se necessário:</p>

    <p style="text-align:center;margin:24px 0">
      <a href="{{ url }}" style="background:#0d6efd;color:#fff;padding:12px 28px;border-radius:4px;text-decoration:none;font-weight:bold">
        Ver solicitação
      </a>
    </p>

    <hr style="border:none;border-top:1px solid #dee2e6;margin:24px 0">
    <p style="font-size:12px;color:#6c757d;margin-bottom:0">
      Este e-mail foi gerado automaticamente pelo ToolboxWaze. Não responda a esta mensagem.
    </p>
  </div>

</body>
</html>
